@model IEnumerable<CitasMedicasFront.Models.Consultorio>

@{
    ViewBag.Title = "Consultorios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="fas fa-clinic-medical text-primary"></i> Gestión de Consultorios</h2>
        <a href="@Url.Action("Crear", "Consultorios")" class="btn btn-success">
            <i class="fas fa-plus-circle"></i> Nuevo Consultorio
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <div class="table-responsive">
                <table id="consultoriosTable" class="table table-striped table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th><i class="fas fa-tag"></i> Nombre</th>
                            <th><i class="fas fa-user-md"></i> Especialidad</th>
                            <th><i class="fas fa-map-marker-alt"></i> Ubicación</th>
                            <th><i class="fas fa-cogs"></i> Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var consultorio in Model)
                        {
                            <tr>
                                <td>@consultorio.Nombre</td>
                                <td>@consultorio.Especialidad</td>
                                <td>@consultorio.Ubicacion</td>
                                <td>
                                    <a href="@Url.Action("Editar", "Consultorios", new { id = consultorio.ConsultorioId })" class="btn btn-warning btn-sm" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="@Url.Action("Eliminar", "Consultorios", new { id = consultorio.ConsultorioId })" class="btn btn-danger btn-sm" title="Eliminar">
                                        <i class="fas fa-trash-alt"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- jQuery -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- DataTables JS + CSS -->
<script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
<link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css" />

<!-- Font Awesome (si no está en tu layout) -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

<script>
    $(document).ready(function () {
        $('#consultoriosTable').DataTable({
            pageLength: 5,
            responsive: true,
            language: {
                url: "https://cdn.datatables.net/plug-ins/1.11.5/i18n/Spanish.json"
            }
        });
    });
</script>
